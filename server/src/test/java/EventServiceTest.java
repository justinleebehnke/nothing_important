public class EventServiceTest {
    /*
    event ID works normally
    event ID throws an error if authtoken is bad
    event ID throws an error if no such event id
    or if the ID is not associated with the user
    */


}
